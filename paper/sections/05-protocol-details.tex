\section{Protocol}
\label{sec:protocol-details}

Ghostext combines a cryptographic packet layer and an LLM token-selection layer. The first protects confidentiality and integrity; the second provides reversible embedding into generated cover text.

\subsection{Operational Semantics}

Alice takes plaintext, passphrase, and shared prompt. Plaintext is wrapped into an authenticated packet, split into \emph{bootstrap} and \emph{body} segments, and mapped to token choices through deterministic candidate selection, quantization, and interval narrowing. Bob tokenizes the received text under the same prompt, replays the same distributions, reconstructs the bootstrap segment first, checks the header, and then decodes the body. Plaintext is released only after full packet authentication. After packet resolution the encoder may append a short natural tail that carries no payload and is ignored by the decoder.

Synchronization is a protocol invariant: runtime settings, backend metadata, and prompt hash are committed into a configuration fingerprint embedded in the authenticated header. Decoding proceeds only when the fingerprint matches local runtime state. The decoder aborts on authentication mismatch, configuration mismatch, synchronization mismatch, unstable tokenization, or token-budget exhaustion, and never emits partial plaintext after authentication failure.

\subsection{Authenticated Packet Layer}

The packet format is
\begin{equation}
\texttt{salt} \parallel \texttt{header\_nonce} \parallel \texttt{sealed\_header} \parallel \texttt{body\_nonce} \parallel \texttt{body\_ciphertext}.
\end{equation}
The internal header stores protocol version, crypto IDs, body length, and configuration fingerprint. The implementation uses scrypt for key derivation, HKDF-SHA256 for key separation, and ChaCha20-Poly1305 for authenticated encryption. Under standard AEAD assumptions and unknown passphrase, packet bytes are intended to be computationally indistinguishable from random given fixed field lengths; the arithmetic-coding layer is thus driven by a bitstream that is secret-independent at the byte level.

\subsection{Candidate Policy and Quantization}

At step $t$, raw logits define base distribution $P_t$ over vocabulary $\mathcal{V}$. Ghostext forms candidate set $C_t$ via deterministic top-$p$/max-$k$ truncation and renormalizes to $\widetilde{P}_t$. If $|C_t|<2$ or step entropy is below threshold $h_{\min}$, no payload bit is consumed. Retokenization-stability filtering then removes candidates that fail render/re-tokenize consistency.

Let $N_t=|C_t|$ and total frequency budget $F_{\mathrm{tot}}$. Each candidate $v\in C_t$ receives $f_t(v)=1+\lfloor \widetilde{P}_t(v)(F_{\mathrm{tot}}-N_t)\rfloor$, with leftover units distributed by fractional remainder and token-id tie-break; $Q_t(v)=f_t(v)/F_{\mathrm{tot}}$. Given interval $[L_t,H_t)$, selecting token $v$ updates
\begin{align}
L_{t+1}&=L_t+\lfloor(H_t-L_t)\,\mathrm{cdf}^-_t(v)\rfloor,\\
H_{t+1}&=L_t+\lfloor(H_t-L_t)\,\mathrm{cdf}^+_t(v)\rfloor.
\end{align}
A segment resolves when $H_t-L_t=1$. Implementation defaults are $F_{\mathrm{tot}}=65536$ and $h_{\min}=1.0$ bit; the released baseline uses $F_{\mathrm{tot}}=4096$ and $h_{\min}=0.0$.

\subsection{Fingerprint Collision}

The fingerprint is the first 64 bits of \texttt{SHA256(JSON(runtime, backend, prompt\_sha256))}. For $n$ honest sessions, $\Pr[\mathrm{collision}]\approx n(n-1)/2^{65}$; for $n=10^6$ this is $\approx 2.7\times10^{-8}$. Fingerprint collisions do not yield valid plaintext without AEAD success. In this paper's passive-censor setting the fingerprint is carried inside the encrypted header rather than exposed on-wire, so it acts as a Bob-local synchronization guard while authenticity remains cryptographic. Widening to 128 bits is the conservative future direction.
