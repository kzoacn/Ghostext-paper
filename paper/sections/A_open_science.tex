\section{Open Science}
This submission's contributions rely on source code, test suites, and experiment scripts. We provide an anonymized artifact bundle containing:
\begin{itemize}
\item Ghostext implementation snapshot used for experiments.
\item Reproduction scripts for correctness/fail-closed/overhead tables.
\item Raw result logs and aggregation scripts.
\end{itemize}

Artifact access details (anonymous URL and checksum) will be inserted in the submission version. If any artifact cannot be shared in full, we will provide a justified partial release that remains sufficient to evaluate core claims.
