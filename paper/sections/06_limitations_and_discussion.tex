\section{Limitations and Discussion}
Current guarantees are conditional on strict sender/receiver synchronization. The system does not currently provide robust recovery under active text edits, channel noise, or model/version drift. Detection resistance against dedicated steganalysis remains an open evaluation direction.

These limitations are deliberate in this version: the design prioritizes deterministic reproducibility and explicit boundary behavior over broad deployment claims.
